<match **>
  @type forward
  send_timeout 60s
  recover_wait 10s
  hard_timeout 60s

  <server>
    name logstash
    host "#{ENV['LOGSTASH_SERVER']}"
    port "#{ENV['LOGSTASH_PORT']}"
  </server>
</match>
